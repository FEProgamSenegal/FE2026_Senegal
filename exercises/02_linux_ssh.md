# Exercice 2 — Linux et SSH

Sur le cluster, créez `~/fe2026/IDENTIFIANT/`, puis un fichier `preuve.txt` contenant le résultat de `hostname`, `whoami`, `date -u` et `pwd`. Copiez-le sur votre ordinateur avec `scp`.

Remettez le fichier et expliquez en une phrase la différence entre votre ordinateur, le head node et un compute node.
