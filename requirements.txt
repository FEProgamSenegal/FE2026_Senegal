# Install after checking compatibility with the cluster Python version.
# Refresh and retest these pins before each course delivery.
emodpy-malaria==3.1.3
idmtools==2.0.3
idmtools-platform-slurm==2.0.3
pandas>=2.2,<3
matplotlib>=3.9,<4
